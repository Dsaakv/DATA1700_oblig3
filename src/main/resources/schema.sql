CREATE TABLE Billett (
                         id INT PRIMARY KEY AUTO_INCREMENT,
                         film VARCHAR(255),
                         antall INT,
                         fornavn VARCHAR(255),
                         etternavn VARCHAR(255),
                         telefonnr VARCHAR(255),
                         epost VARCHAR(255)
);